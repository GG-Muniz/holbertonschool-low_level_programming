#include "main.h"

/**
 * main - program will print '_putchar' followed by a new line.
 * Return: 0
*/
int main(void)
{
	char *p = "_putchar";
	int i;

	for (i = 0; i <= p[i]; i++)
	{
		_putchar(p[i]);
	}
	_putchar('\n');
	return (0);
}
