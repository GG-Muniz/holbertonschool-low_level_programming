#include "main.h"
#include <stdio.h>
/**
 * mul - multiplies two integers
 * @a: is the integer to multiply.
 * @b: is the integer to multiply.
 * Return: result.
*/
int mul(int a, int b)
{
	int r;

	r = a * b;

	return (r);
}
