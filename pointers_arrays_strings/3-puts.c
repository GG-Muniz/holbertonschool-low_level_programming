#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _puts - prints a string followed by a new line.
 * @str: reference to the string to print.
*/
void _puts(char *str)
{
	printf("%s\n", str);
}
